import { DeepPartial, Theme } from '@chakra-ui/react'

const fonts: DeepPartial<Theme['fonts']> = {
  body: '-apple-system, system-ui, BlinkMacSystemFont, Lexend, sans-serif',
  heading: '-apple-system, system-ui, BlinkMacSystemFont, Lexend, serif',
}

export default fonts
